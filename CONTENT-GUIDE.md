# 内容管理指南

本文档说明如何更新网站内容（无需修改代码）。

## 📁 数据文件结构

```
data/
├── journals.json      # 日志内容
├── plans.json         # 计划目标
├── about.json         # 个人信息
└── gallery/           # 相册数据
    ├── hiking.json    # 徒步相册
    ├── travel.json    # 旅游相册
    ├── cycling.json   # 骑行相册
    ├── crocheting.json # 编织作品
    └── painting.json  # 绘画作品
```

## 📝 添加新日志

编辑 `data/journals.json`，在 `journals` 数组中添加：

```json
{
  "id": "journal-20260301",
  "title": "我的新日志标题",
  "category": "study",
  "categoryName": "学习",
  "excerpt": "日志摘要（150字以内）...",
  "content": "完整内容，支持多段落。\n\n段落之间用两个换行分隔。",
  "tags": ["标签1", "标签2"],
  "coverImage": "images/blog/my-cover.jpg",
  "createdAt": "2026-03-01",
  "readTime": 3,
  "viewCount": 0,
  "isTop": false,
  "status": "published"
}
```

### 分类选项
- `study` - 学习
- `life` - 生活  
- `work` - 工作

## 🎯 更新计划

编辑 `data/plans.json`，在各类型数组中添加：

```json
{
  "id": "plan-short-new",
  "title": "新目标名称",
  "description": "目标描述",
  "progress": 0,
  "status": "pending",
  "deadline": "2026-06-01",
  "priority": 3,
  "createdAt": "2026-03-01"
}
```

### 计划类型
- `short-term` - 短期目标（3个月内）
- `medium-term` - 中期目标（1年内）
- `long-term` - 长期目标（3-5年）

### 状态选项
- `pending` - 待开始
- `in-progress` - 进行中
- `completed` - 已完成

## 🖼️ 添加相册照片

### 步骤

1. **上传图片文件**
   ```
   images/
   └── hiking/
       └── photo-name.jpg
   ```

2. **更新JSON数据**
   编辑 `data/gallery/hiking.json`：
   ```json
   {
     "photos": [
       {
         "id": "photo-001",
         "url": "images/hiking/photo-name.jpg",
         "thumbnail": "images/hiking/thumb/photo-name.jpg",
         "caption": "照片描述",
         "location": "拍摄地点",
         "takenAt": "2026-02-20",
         "order": 1
       }
     ]
   }
   ```

## 📸 图片上传规范

### 支持格式
- JPG/JPEG
- PNG
- GIF
- WebP
- SVG

### 文件大小
- 无限制
- 建议单张不超过5MB
- 封面图建议 16:9 比例

### 目录建议
```
images/
├── profile/          # 个人头像
├── blog/             # 日志封面
├── hiking/           # 徒步照片
├── travel/           # 旅游照片
├── cycling/          # 骑行照片
├── crocheting/       # 编织作品
├── painting/         # 绘画作品
└── hobbies/          # 其他爱好
```

## 🔄 版本控制

### 提交内容变更

```bash
# 查看内容变更状态
node scripts/content-version.js status

# 提交内容变更
node scripts/content-version.js commit "添加新日志"

# 查看提交历史
node scripts/content-version.js log
```

### Git 操作

```bash
# 推送到远程（同步到其他设备）
git push origin main

# 从远程拉取（获取其他设备的更新）
git pull origin main
```

## 💡 最佳实践

### 更新频率
- 每周 2-4 次为宜
- 重要内容及时更新

### 更新流程
1. 修改 JSON 文件
2. 刷新网页查看效果
3. 确认无误后提交版本

### 多设备同步
1. 在设备A修改内容
2. 执行 `git push`
3. 在设备B执行 `git pull`
4. 刷新网页即可看到更新

## ⚠️ 注意事项

1. **JSON格式**
   - 注意逗号分隔
   - 最后一个元素不加逗号
   - 使用英文引号

2. **文件路径**
   - 使用相对路径
   - 路径区分大小写

3. **编码**
   - 文件使用 UTF-8 编码
   - 支持中文内容

4. **备份**
   - Git 会自动保存历史版本
   - 可随时回滚到之前版本
