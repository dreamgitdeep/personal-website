# 我的个人空间 💜

一个基于原生前端技术栈构建的唯美紫色系个人展示网站，采用 JSON 数据驱动的内容管理方案，支持响应式布局与丰富的交互动效。

---

## 📋 目录

- [项目概述](#项目概述)
- [功能模块](#功能模块)
- [技术架构](#技术架构)
- [文件结构](#文件结构)
- [核心实现](#核心实现)
- [项目特色](#项目特色)
- [本地运行](#本地运行)
- [部署指南](#部署指南)

---

## 项目概述

本项目是一个纯前端个人网站，无需后端服务器即可运行。采用 **JSON 数据驱动** 的内容管理方案，只需修改配置文件即可更新网站内容，非常适合快速部署到静态托管平台。

**技术选型：**
- 纯 HTML5 + CSS3 + JavaScript（ES6+）
- 无框架依赖，轻量高效
- 数据与视图分离，便于维护

---

## 功能模块

### 1. 首页 (`index.html`)
- 个人头像与简介展示
- 打字机效果的动态标语
- 浮动形状动画背景
- 导航入口卡片

### 2. 关于我 (`about.html`)
- **个人简介卡片**：头像、姓名、标语、个人故事
- **教育背景时间轴**：学历信息展示
- **工作经历时间轴**：实习/工作经历
- **项目经历时间轴**：科研项目、竞赛项目等
- **图片画廊模态框**：支持缩略图导航、键盘/触摸滑动切换

### 3. 日志 (`blog.html`)
- 文章列表卡片展示
- 分类筛选（学习/生活/工作）
- 关键词搜索功能
- 文章详情弹窗
- 数据来源：`data/journals.json`

### 4. 计划 (`plans.html`)
- **目标看板**：短期（3个月）、中期（1年）、长期（3-5年）目标分类展示
- **进度条可视化**：每个目标配有进度指示
- **统计面板**：已完成、进行中、待开始数量统计
- 数据来源：`data/plans.json`

### 5. 联系我 (`contact.html`)
- **联系信息展示**：邮箱（支持一键复制）、所在城市、工作时间
- **居中单列布局**：内容已调整为单列并水平居中，适配更简洁的视觉效果


---

## 技术架构

### CSS 架构

采用 **CSS 变量系统**（Design Tokens）实现统一的设计语言：

```css
:root {
  /* 主色调 - 柔和紫色系 */
  --primary-light: #e8d5ff;
  --primary: #c084fc;
  --primary-dark: #a855f7;
  --primary-deep: #9333ea;

  /* 辅助色 - 柔和粉色系 */
  --accent-pink: #f9a8d4;
  --accent-rose: #f472b6;

  /* 间距系统 - 8点网格 */
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;

  /* 圆角系统 */
  --radius-sm: 6px;
  --radius-md: 10px;
  --radius-lg: 16px;

  /* 阴影系统 */
  --shadow-sm: 0 1px 2px rgba(147, 51, 234, 0.05);
  --shadow-md: 0 4px 6px rgba(147, 51, 234, 0.07);
  --shadow-glow: 0 0 15px rgba(192, 132, 252, 0.3);
}
```

**样式组织：**
- `css/style.css`：全局样式、导航栏、页脚、通用组件、响应式断点
- `css/about.css`：关于我页面专属样式（时间轴、爱好卡片）
- `css/blog.css`：日志页面专属样式
- `css/contact.css`：联系页面专属样式（留言板）
- `css/plans.css`：计划页面专属样式（目标看板）
- `css/*-gallery.css`：各相册页面专属样式

### JavaScript 架构

**模块划分：**

| 文件 | 职责 |
|------|------|
| `js/main.js` | 全局功能：导航菜单、打字机效果、滚动动画、返回顶部、页面过渡、图片懒加载 |
| `js/about.js` | 加载 `about.json` 动态渲染个人信息、兴趣爱好卡片、图片画廊 |
| `js/blog.js` | 日志筛选、搜索、详情弹窗 |
| `js/plans.js` | 目标数据渲染、进度动画 |
| `js/contact.js` | 留言板系统：发表/回复/点赞/删除 |
| `js/*-gallery.js` | 相册功能：瀑布流渲染、模态框、照片导航、备注编辑 |

**核心技术实现：**

```javascript
// IntersectionObserver 实现滚动动画
const observer = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      entry.target.classList.add('active');
    }
  });
}, { threshold: 0.1 });

// 打字机效果
function type() {
  const currentText = texts[textIndex];
  typingElement.textContent = currentText.substring(0, charIndex + 1);
  if (!isDeleting && charIndex === currentText.length) {
    isDeleting = true;
    typingSpeed = 2000;
  }
  setTimeout(type, typingSpeed);
}

// LocalStorage 数据持久化
function saveMessage(message) {
  let messages = getMessages();
  messages.unshift(message);
  localStorage.setItem('guestbookMessages', JSON.stringify(messages));
}
```

---

## 文件结构

```
personal-website/
├── index.html              # 首页
├── about.html              # 关于我
├── blog.html               # 日志
├── plans.html              # 计划
├── contact.html            # 联系我
<!-- 相册页面已移除 -->
│
├── css/
│   ├── style.css           # 全局样式（CSS变量、导航、页脚、响应式）
│   ├── about.css           # 关于我页面样式
│   ├── blog.css            # 日志页面样式
│   ├── contact.css         # 联系页面样式
│   ├── plans.css           # 计划页面样式
│   └── *-gallery.css       # 各相册页面样式
│
├── js/
│   ├── main.js             # 全局脚本（导航、打字机、滚动动画、懒加载）
│   ├── about.js            # 关于我页面脚本
│   ├── blog.js             # 日志页面脚本
│   ├── contact.js          # 联系页面脚本（留言板）
│   ├── plans.js            # 计划页面脚本
│   └── *-gallery.js        # 各相册页面脚本
│
├── data/
│   ├── about.json          # 个人信息配置
│   ├── journals.json       # 日志文章数据
│   ├── plans.json          # 计划目标数据
│   ├── travel-photos-data.js   # 旅游照片配置
│   ├── hiking-photos-data.js   # 徒步照片配置
│   └── gallery/            # 各相册JSON配置（若保留相册则存放配置）
│
├── images/
│   ├── profile/            # 个人头像
│
├── scripts/
│   └── scan-travel-photos.js   # 照片扫描脚本（Node.js）
│
└── package.json            # 项目配置
```

---

## 核心实现

### 1. JSON 数据驱动

网站内容通过 JSON 文件配置，实现数据与视图分离：

**个人信息配置 (`data/about.json`)：**
```json
{
  "profile": {
    "name": "秋千",
    "title": "记录成长 · 分享想法 · 规划未来 💜",
    "avatar": "images/profile/avatar.jpg"
  },
  "hobbies": []
}
```

**日志数据 (`data/journals.json`)：**
```json
{
  "journals": [
    {
      "id": "journal-20260223",
      "title": "我的网站制作初体验",
      "category": "study",
      "excerpt": "文章摘要...",
      "content": "文章内容...",
      "tags": ["网站制作", "学习"],
      "createdAt": "2026-02-23"
    }
  ]
}
```

### 2. 响应式设计

采用移动优先的响应式策略：

```css
/* 响应式断点 */
@media (max-width: 768px) {
  /* 平板适配 */
  .nav-menu {
    position: fixed;
    top: 60px;
    left: -100%;
    /* 移动端抽屉菜单 */
  }
}

@media (max-width: 480px) {
  /* 手机适配 */
  .hero-title { font-size: 2rem; }
}
```

### 3. 性能优化

- **图片懒加载**：使用 `IntersectionObserver` 延迟加载图片
- **CSS 动画硬件加速**：`transform` 和 `opacity` 触发 GPU 加速
- **防抖节流**：滚动事件使用 `debounce` 优化
- **骨架屏/占位图**：图片加载前显示占位

```javascript
// 图片懒加载实现
const imageObserver = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      const img = entry.target;
      img.src = img.dataset.src;
      observer.unobserve(img);
    }
  });
});
```

---

## 项目特色

### 1. 唯美视觉设计
- 柔和紫色系配色，统一的设计语言
- 毛玻璃效果（`backdrop-filter: blur(10px)`）
- 渐变背景与浮动形状动画
- 圆润柔和的卡片设计

### 2. 丰富交互动效
- 打字机效果（首页标语）
- 滚动入场动画（IntersectionObserver）
- 卡片悬停效果
- 页面过渡动画
- 粒子效果背景

### 3. 完善的移动端适配
- 响应式导航菜单（汉堡菜单）
- 触摸滑动切换照片
- 自适应布局

### 4. 数据持久化
- 留言板数据存储于 LocalStorage
- 照片备注本地保存
- 昵称记忆功能

### 5. 无障碍支持
- 语义化 HTML 标签
- 图片 `alt` 属性
- 键盘导航支持

---

## 本地运行

### 方式一：直接打开

直接在浏览器中打开 `index.html` 文件即可。

> 注意：部分功能（如 fetch 加载 JSON）需要 HTTP 服务器环境。

### 方式二：本地服务器

```bash
# 安装依赖
npm install

# 方式1: 使用 http-server
npm start
# 访问 http://localhost:8080

# 方式2: 使用 live-server（支持热重载）
npm run dev
# 自动打开浏览器
```

---

## 部署指南

### GitHub Pages 部署

1. 创建 GitHub 仓库
2. 推送代码：
   ```bash
   git remote add origin https://github.com/你的用户名/personal-website.git
   git push -u origin main
   ```
3. 开启 GitHub Pages：
   - 进入仓库 Settings → Pages
   - Source 选择 `Deploy from a branch`
   - Branch 选择 `main`，文件夹选择 `/ (root)`
4. 访问地址：`https://你的用户名.github.io/personal-website`

> **Tip:** 已包含 `deploy.ps1`（PowerShell）和 `deploy.sh`（bash）脚本，
> 可快速提交并推送更改：
> ```powershell
> .\deploy.ps1 -Message "更新内容说明"
> ```
> 或
> ```bash
> ./deploy.sh "更新内容说明"
> ```

### 其他部署平台

| 平台 | 特点 |
|------|------|
| **Gitee Pages** | 国内访问快，注册简单 |
| **Vercel** | 支持 GitLab 登录，自动部署 |
| **Netlify** | 免费 SSL，表单处理 |

---

## 内容更新

### 更新个人信息
编辑 `data/about.json` 文件

### 添加日志文章
编辑 `data/journals.json` 文件

### 添加照片
（兴趣相册已移除，若要恢复请将照片存放于合适目录并更新配置）

---

## 技术栈

| 技术 | 用途 |
|------|------|
| HTML5 | 页面结构 |
| CSS3 | 样式设计（CSS变量、Flexbox、Grid、动画） |
| JavaScript (ES6+) | 交互逻辑 |
| Font Awesome 6.4 | 图标库 |
| Google Fonts (Noto Sans SC) | 中文字体 |
| LocalStorage API | 本地数据存储 |
| IntersectionObserver API | 滚动动画、懒加载 |

---

## 浏览器支持

- Chrome 80+
- Firefox 75+
- Safari 13+
- Edge 80+

---

Made with 💜 by 秋千
